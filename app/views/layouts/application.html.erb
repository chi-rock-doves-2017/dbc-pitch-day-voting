<!DOCTYPE html>
<html>
  <head>
    <title>DbcPitchDayVoting</title>
    <%= csrf_meta_tags %>

    <%= stylesheet_link_tag    'application', media: 'all' %>
    <%= javascript_include_tag 'application' %>
  </head>

  <body class="badge--blue-lite">
    <nav class="nav">
      <% if logged_in? %>
      <%= link_to 'Log Out', logout_path, method: :delete %>
      <% else %>
      <a id="login-github" href="/auth/github"><button class="login button-primary button--teal">Sign in with Github</button></a>
      <% end %>
    </nav>
    <%= yield %>
  </body>
</html>
